import { PureComponent } from 'react';

class ContextMenu extends PureComponent {
  // Just a shell component for clubbing menu items together
  // All context menu logic is in ContextMenuArea
}

export default ContextMenu;
